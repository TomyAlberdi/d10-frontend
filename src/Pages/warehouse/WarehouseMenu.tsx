const WarehouseMenu = ({
  currentLevel,
  handleLevelUp,
  handleLevelDown,
}: {
  currentLevel: number;
  handleLevelUp: () => void;
  handleLevelDown: () => void;
}) => {
  return <div className="border border-red-500">WarehouseMenu</div>;
};

export default WarehouseMenu;
